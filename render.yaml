services:
  - type: web
    name: faj-inventory-system
    env: python
    region: oregon
    branch: main
    root: .
    buildCommand: bash build.sh
    startCommand: gunicorn inventory_system.wsgi:application --bind 0.0.0.0:$PORT
